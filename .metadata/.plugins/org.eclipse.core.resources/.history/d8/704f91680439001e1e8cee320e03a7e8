package bar.store;

import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;

import bar.employee.Employee;

public class GangBar {
	String name; // 가게 이름
	int totalSell; // 가게 수입
	
	@Autowired(required = false)
	List<Employee> tables; // 직원
	List<Employee> emps; // 직원
}
