package bar;

import bar.menu.Alcohol;
import bar.menu.AlcoholSet;
import bar.menu.DrinkDish;

public interface GangnamBar {
	void make();
	void serving();
	int menuCalc(Alcohol al, DrinkDish drd, AlcoholSet alSet);
	void ordered(String order);
}
